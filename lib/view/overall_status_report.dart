import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class OverallStatusReportPage extends StatelessWidget {
  const OverallStatusReportPage({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text("Overall Status Report")));
}